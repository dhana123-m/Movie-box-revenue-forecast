# Development / training dependencies = serve-time deps plus tooling.
-r requirements.txt

# Deep learning training (Keras). Also provides tf.lite for converting the
# trained model:  python scripts/convert_to_tflite.py
tensorflow-cpu>=2.19,<3.0

# Boosting baseline in the training pipeline (optional, falls back to
# sklearn GradientBoosting when missing)
xgboost>=2.1

# Evaluation plots
matplotlib>=3.9
seaborn>=0.13

# Testing
pytest>=8.0
httpx>=0.27
